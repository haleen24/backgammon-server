spring:
  application:
    name: backgammon-game

server:
  error:
    include-message: always
  port: 82
  servlet:
    context-path: /game

config:
  sse:
    time-out: ${GAME_SSE_TIMEOUT:#{3600}}

  game:
    after-close-delay: 120